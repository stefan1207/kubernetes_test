apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base 

patches:
  - target:
      kind: Ingress
      name: demo-ingress
    patch: |
      - op: replace
        path: /spec/rules/0/paths/0/path
        value: demo-test
